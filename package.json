{
  "name": "meme-labeling-server",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "label": "bun run src/label.ts",
    "export:labels": "bun run scripts/export-labels.ts labels.json",
    "db:reset": "bun run scripts/reset-db.ts",
    "db:migrate": "bun run scripts/migrate-schema.ts",
    "embed": "bun run src/embed.ts",
    "test": "bun test",
    "test:embed": "bun test src/embed.labels.stress.test.ts",
    "pipeline": "bun run src/pipeline.ts",
    "pipeline:remote": "SERVER_URL=$SERVER_URL bun run src/pipeline.ts",
    "compare:rerank": "bun run scripts/compare-rerank-hinglish.ts"
  },
  "dependencies": {
    "@ai-sdk/google": "^0.0.50",
    "@libsql/client": "^0.17.3",
    "ai": "^3.0.0",
    "hono": "^4.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}